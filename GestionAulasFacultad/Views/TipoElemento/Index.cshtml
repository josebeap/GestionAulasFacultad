@model IEnumerable<GestionAulasFacultad.Models.ModeloTipoElementoGUI>

@{
    ViewBag.Title = "Index";
}

<h2 class="d-flex justify-content-center">Lista de tipos de elementos del inventario</h2>

<br />

<div class="row d-flex justify-content-center">
    <div class="col-4.5">

        <table class="table table-responsive table-bordered">
            <thead class="table-dark">
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.Nombre)
                    </th>
                    <th>Opcion</th>
                </tr>
            </thead>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Nombre)
                    </td>
                    <td>

                        <div class="row">
                            <div class="col-4">

                                @Html.ActionLink(" ", "Edit", new { id = item.Id }, new { @class = "btn btn-primary fa fa-pencil" })

                            </div>
                            <div class="col-4">

                                @Html.ActionLink(" ", "Details", new { id = item.Id }, new { @class = "btn-lg btn-warning fa fa-info" })

                            </div>
                            <div class="col-4">

                                @Html.ActionLink(" ", "Delete", new { id = item.Id }, new { @class = "btn btn-danger fa fa-trash" })

                            </div>
                        </div>
                    </td>
                </tr>
            }

        </table>
        @Html.Partial("_FooterPagedList")

        @Html.ActionLink("Crear tipo de elemento", "Create", new { }, new { @class = "btn btn-success btn-lg d-flex justify-content-center" })
    </div>
</div>


